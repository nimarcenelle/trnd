export default function Home() {
  // Placeholder — replaced by the full landing page port in a later milestone.
  return (
    <main className="wrap" style={{ padding: "96px 32px" }}>
      <span className="eyebrow">Live signal → finished campaign</span>
      <h1 className="h-disp" style={{ fontSize: "clamp(36px,4.6vw,58px)", lineHeight: 1.04 }}>
        Know what to advertise — <em style={{ fontStyle: "normal", color: "var(--amber)" }}>before your competitors do.</em>
      </h1>
    </main>
  );
}
