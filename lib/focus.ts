/**
 * Focus mode: the product shows the category read and the week's tests,
 * and nothing else asks for attention. Everything hidden still exists and
 * still works by URL; flip this to bring back the full landing page and
 * the Campaigns, Track record and Snapshot tabs.
 */
export const FOCUS_MODE = true;
